#ifndef CONNECTION_H
#define CONNECTION_H

// the connection only has one variable: charge
// there are no functions etc. needed, hence no implementation file
struct Connection
{
    double charge{};
};

#endif